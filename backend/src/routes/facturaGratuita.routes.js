/**
 * Rutas para facturas gratuitas
 */


/**
const express = require('express');
const router = express.Router();
const facturaGratuitaController = require('../controllers/facturaGratuita.controller');
const { verificarToken } = require('../middlewares/authMiddleware');
 */

//router.post('/', verificarToken, facturaGratuitaController.crearFacturaGratuita);


//router.get('/info', verificarToken, facturaGratuitaController.getInfoFacturaGratuita);

//module.exports = router;