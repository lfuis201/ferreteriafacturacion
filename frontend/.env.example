# Copia este archivo como .env y completa la URL del backend
# En Windows: copy .env.example .env
# En Linux/Mac: cp .env.example .env

# URL de la API (backend). Local: http://localhost:4000
VITE_API_URL=http://localhost:4000/api
